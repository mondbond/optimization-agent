name: sup_availability_description
versions:
  - version: 1
    alias: base
    template: |
      Use this if user talks about entity that suppose to supply/provide/can produce something in an abstract or specific way.
      You need to mention supplier name in key1 and the amount of items it can supply in value. If user just mention the supplier without specifying the amount, you can set the value to empty string.
